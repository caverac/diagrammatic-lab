{
  "name": "@diagrammatic-lab/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure TypeScript algebra engine for diagrammatic algebra",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b && tsc-alias -p tsconfig.json",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json --noEmit",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.3",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  }
}
