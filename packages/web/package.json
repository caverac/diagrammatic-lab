{
  "name": "@diagrammatic-lab/web",
  "version": "0.0.0",
  "private": true,
  "description": "Browser UI for the Temperley–Lieb explorer (Phase 2 — UI not yet built)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@diagrammatic-lab/core": "workspace:^",
    "@diagrammatic-lab/renderer": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
